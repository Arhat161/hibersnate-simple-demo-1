CREATE TABLE Person (
                        id int PRIMARY KEY,
                        name varchar(100),
                        age int
);

insert into person(id, name, age) values (1, 'Test Person', 20);